{
  "name": "launcher-server",
  "version": "0.1.0",
  "dependencies": {
    "redis": "~0.8.4",
    "express.io": "~1.1.13"
  },
  "devDependencies": {
    "pm2": "~0.5.7"
  },
  "scripts": {
    "start": "redis-server redis/redis.conf && pm2 start app.js && pm2 logs",
    "stop": "pm2 stop app.js && redis-cli shutdown"
  }
}
